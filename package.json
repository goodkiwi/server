{
  "name": "typing-server",
  "version": "1.0.0",
  "description": "A typing test server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^4.0.0"
    // other dependencies
  },
  "devDependencies": {
    // dev dependencies if any
  },
  "author": "Your Name",
  "license": "ISC"
}
